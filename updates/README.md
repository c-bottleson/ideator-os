# Ideator OS — updates (for bots)

Dated patches Ideator can **propose** (never auto-apply).

Weekly check prompt (for Ideator routines):
> Fetch recent files under `updates/` and GitHub Releases on c-bottleson/ideator-os. Propose 1–3 that fit *this* user’s workflow. Do not change memories or skills until they say yes.

See also: `../newsletter/` for the human-facing weekly.
